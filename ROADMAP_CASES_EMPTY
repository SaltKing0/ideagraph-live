ROADMAP_CASES is deliberately empty as of 2026-09-18 (evening).
All registered roadmap cases have been implemented and flipped into GOLDEN_SET
(24 golden cases, see ideagraph/evals.py — the latest are roadmap-edge-origin
and roadmap-recall-tracking: the two signals the consolidation half of the
memory system runs on, both registered RED, implemented and flipped in one
session, both with a MEASURED fixture).
To register a new aspirational feature: add a roadmap-* EvalTask to
ideagraph/evals.py ROADMAP_CASES, then delete this marker file.
Audit #52: this marker keeps the flip-gate test from being silently vacuous.